import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  recipeView: {
    position: 'relative',
    width: '100%',
    height: '100%',
    flex: 1,
  },
  titleStyle: {
    fontSize: 20,
    marginLeft: '10%',
    fontWeight: '800',
    marginBottom: 10,
  },
});

export default styles;
